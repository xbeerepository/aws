schema-version: 1.0

type: provider

provider: https://s3.eu-west-3.amazonaws.com/xbee.repository/providers/{{ .xbee.os }}_{{ .xbee.arch }}/{% xbeeName %}-{% version %}.tar.gz

var:
  region: eu-west-3
  type: t2.nano
  size: 20
  amis: #to be filled with system product

command:
  hostdata: "{{ .templates }}/hostdata.sh"
  